# 0x13. JavaScript - Objects, Scopes and Closures

## 0. Rectangle #0

An empty class `Rectangle` that defines a rectangle.

### Usage
```javascript
const Rectangle = require('./0-rectangle');
const r1 = new Rectangle();EOF

